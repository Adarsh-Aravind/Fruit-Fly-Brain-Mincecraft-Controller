# Python 3.12. PyTorch must come from the CUDA wheel index:
#   python -m uv pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu128 --extra-index-url https://pypi.org/simple
torch==2.11.0
numpy==2.5.3
pandas==3.0.5
pyarrow==25.0.1
websockets==17.1
aiohttp==3.14.3
